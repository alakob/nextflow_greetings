params.greetings = "greetings.txt"
params.name = "World"
params.outdir = "results"

docker.enabled = true

process {
       cpus = 1
        memory = '512 MB'
        time = '1h'

    resourceLimits = [
        'cpu': 1,
        'memory': '512 MB',
        'time': '1h'
    ]
}